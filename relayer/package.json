{
  "name": "@perihelion/relayer",
  "version": "0.0.1",
  "description": "LayerZero message relayer for the Perihelion Stellar <-> EVM path",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "perihelion-relayer": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "node --test --import tsx test/*.test.ts",
    "test:coverage": "c8 --reporter=lcov --reporter=text --report-dir=coverage node --test --import tsx test/*.test.ts",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@perihelion/sdk": "*",
    "@stellar/stellar-sdk": "^12.0.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "c8": "^10.1.2",
    "tsx": "^4.16.0",
    "typescript": "^5.4.0"
  }
}
