openapi: 3.1.0

info:
  title: Perihelion Mempool API
  version: 1.0.0
  contact:
    name: Perihelion Protocol
    url: https://github.com/Perihelion-Protocol/Perihelion
  description: |
    The Perihelion Mempool API is the HTTP surface through which clients
    submit signed intents, query their lifecycle status, and list intents
    by status.

    ## Intent lifecycle

    The full cross-chain intent lifecycle (as modeled by the SDK and the
    on-chain contracts) has six states:

    ```
    pending → claimed → settling → settled
           ↘                    ↘
            expired              refunded
    ```

    | Status     | Meaning                                                      |
    |------------|--------------------------------------------------------------|
    | `pending`  | Signed, in the mempool, unclaimed by any solver              |
    | `claimed`  | A solver has locked the source-chain funds                   |
    | `settling` | Cross-chain LayerZero message is in flight                   |
    | `settled`  | Assets released on Stellar; bridge complete                  |
    | `refunded` | Solver-initiated refund after a failed or cancelled fill     |
    | `expired`  | Deadline passed before any solver claimed the intent         |

    **This mempool's `IntentStatus` only covers four of those six states:**
    `pending`, `settled`, `refunded`, `expired`. `claimed` and `settling` are
    part of the broader on-chain/SDK lifecycle but are never assigned by this
    reference implementation — solvers and the relayer currently track those
    intermediate states out-of-band.

    ## Status ownership

    The mempool does not observe chain state itself. Status transitions are
    reported by trusted relayer/solver services via
    `PATCH /intents/{hash}/status`, authenticated with a bearer token shared
    out-of-band (the mempool operator's `statusToken`). If no token is
    configured, the endpoint is unauthenticated — suitable only for local
    development. Mempool status is a convenience hint for polling clients; it
    is not a substitute for verifying settlement on-chain.

    ## Pagination

    `GET /intents` returns an envelope `{ records, nextCursor }`. Pass the
    `nextCursor` from one response as the `cursor` query parameter of the next
    request to page forward; `nextCursor` is absent once the last page has
    been returned. A `cursor` that no longer resolves to a record — the record
    it named was evicted by the expiry sweep or the size cap, or a status
    change moved it out of a status-filtered result set — is rejected with
    `400` and `code: "unresolvable_cursor"` so a client can restart
    pagination deliberately instead of being silently sent back to page one.

    ## Error responses

    All error responses share the shape `{ "error": "<message>" }` and use
    standard HTTP status codes. This holds for errors raised by the HTTP
    framework as well as by application code: a body that is not valid JSON
    returns `400`, a body above the 8 KiB limit returns `413` with a message
    naming the limit, and a request to an unknown route or method returns
    `404`. No response is ever an HTML error page, and none includes a stack
    trace.

    **Known gaps** (each tracked by its own issue, not yet part of this
    contract):
    - Duplicate submission: `POST /intents` currently overwrites the existing
      record for a hash that has already been submitted (last write wins) and
      returns `200`, rather than rejecting the second submission with `409`.
      Tracked by issue #320.
    - (resolved, #348/#561) Both `GET /intents/{hash}` and
      `PATCH /intents/{hash}/status` now normalise the `:hash` to lower case
      and reject a value that is not a `0x`-prefixed 32-byte hex string with
      `400`, so a malformed hash is distinguishable from an unknown one on
      every route that takes a hash.

servers:
  - url: http://localhost:3000
    description: Local development server (default port)

tags:
  - name: Intents
    description: Submit and query cross-chain bridge intents
  - name: Discovery
    description: Server metadata and EIP-712 domain discovery

paths:

  /intents:

    post:
      operationId: submitIntent
      tags: [Intents]
      summary: Submit a signed intent
      description: |
        Validates the EIP-712 signature over the intent, computes the canonical
        `intent_hash`, and stores the record with status `pending`.

        The server rejects the submission if the signature is invalid or any
        required field is missing (`400`).

        **Duplicate submissions are not currently rejected.** If an intent
        with the same hash has already been submitted, this call overwrites
        the stored record (last write wins) and still returns `200`. A
        strict `409 Conflict` check is tracked separately by issue #320 and
        is not yet part of this contract.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignedIntent'
            example:
              intent:
                user: "0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045"
                destination: "GBBD47IF6LWK7P7MDEVSCWR7DPUWV3NY3DTQEVFL4NAT4AQH3ZLLFLA5"
                sourceChainId: 8453
                sourceAsset: "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913"
                sourceAmount: "1000000"
                destAsset: "USDC:GA5ZSEJYB37JRC5AVCIA5MOP4RHTM335X2KGX3IHOJAPP5RE34K4KZVN"
                minDestAmount: "9900000"
                deadline: 1751280000
                nonce: "95395590780195993686340100862393633005949636859136019347794446361576576451305"
                preferredSolver: "0x0000000000000000000000000000000000000000"
              signature: "0x1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b0f1e2d3c4b5a69780f1e2d3c4b5a69780f1e2d3c4b5a69780f1e2d3c4b5a69781c"
              hash: "0x1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b"
      responses:
        '200':
          description: |
            Intent accepted and stored; returns the canonical intent hash.
            Also returned when overwriting a prior submission with the same
            hash (see the duplicate-submission note above).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmitIntentResponse'
              example:
                hash: "0x1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b"
        '400':
          description: |
            Validation failure — missing required fields, invalid EIP-712
            signature, or a request body that is not well-formed JSON.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                missingFields:
                  summary: Missing intent or signature
                  value:
                    error: "Missing intent or signature"
                invalidSignature:
                  summary: Invalid EIP-712 signature
                  value:
                    error: "Invalid signature"
                expiredDeadline:
                  summary: Deadline has already passed
                  value:
                    error: "Intent deadline has passed"
                malformedJson:
                  summary: Request body is not valid JSON
                  value:
                    error: "Malformed JSON request body"
        '409':
          description: |
            Conflict — an intent with this hash already exists in the mempool.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: "Intent already exists"
        '413':
          description: |
            The JSON request body exceeds the server's 8 KiB limit. The
            message names the limit so a client can distinguish a size
            rejection from a parse failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: "Request body exceeds the 8kb limit"
        '500':
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: "Internal server error"

    get:
      operationId: listIntents
      tags: [Intents]
      summary: List intents, optionally filtered by status and/or chain
      description: |
        Returns a page of intent records in the mempool. Pass the optional
        `status` query parameter to filter to a single lifecycle state.

        Results are paginated: pass `cursor` (the last `hash` from the
        previous page) to fetch the next page. A `nextCursor` is included in
        the response while more records remain. An optional `chainId`
        filter narrows results to a single source chain.
      parameters:
        - name: status
          in: query
          required: false
          description: |
            Filter results to intents in this lifecycle state. When omitted,
            all intents are returned regardless of status. Must be passed at
            most once; repeating the parameter (`?status=a&status=b`) is
            rejected rather than silently matching nothing.
          schema:
            $ref: '#/components/schemas/IntentStatus'
        - name: limit
          in: query
          required: false
          description: |
            Maximum records to return. Defaults to 100; values above 1000 are
            capped. Must be a positive integer and passed at most once — a
            non-integer, non-positive, or repeated value is rejected with 400
            rather than silently replaced with the default.
          schema:
            type: integer
            minimum: 1
        - name: cursor
          in: query
          required: false
          description: |
            The `hash` of the last record from the previous page. Must be
            passed at most once.
          schema:
            type: string
        - name: chainId
          in: query
          required: false
          description: |
            Filter results to intents whose `intent.sourceChainId` equals
            this value. Useful for a mempool instance that may see intents
            destined for more than one source chain's escrow. Must be a
            positive integer and passed at most once — a non-numeric or
            repeated value is rejected with 400 rather than returning an
            empty page indistinguishable from "no intents match".
          schema:
            type: integer
            minimum: 1
          example: 8453
      responses:
        '200':
          description: A page of intent records.
          content:
            application/json:
              schema:
                type: object
                properties:
                  records:
                    type: array
                    items:
                      $ref: '#/components/schemas/IntentRecord'
                  nextCursor:
                    type: string
                    nullable: true
                    description: Pass as `cursor` to fetch the next page. Absent on the last page.
        '400':
          description: |
            Validation failure on a query parameter: `status` is not one of
            the `IntentStatus` enum values, `limit` is not a positive integer,
            `chainId` is not a positive integer, or any of `status`, `limit`,
            `chainId`, or `cursor` was passed more than once.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                invalidStatus:
                  summary: Unrecognized status value
                  value:
                    error: "status must be one of pending, settled, refunded, expired"
                repeatedStatus:
                  summary: status parameter passed more than once
                  value:
                    error: "status must be one of pending, settled, refunded, expired"
                invalidLimit:
                  summary: limit is not a positive integer
                  value:
                    error: "limit must be a positive integer"
                invalidChainId:
                  summary: chainId is not a positive integer (or was repeated)
                  value:
                    error: "chainId must be a positive integer"

  /intents/{hash}:

    get:
      operationId: getIntent
      tags: [Intents]
      summary: Get an intent record by hash
      description: |
        Retrieves the full `IntentRecord` for the given `intent_hash`,
        including current lifecycle status and any solver/settlement metadata
        that has been attached since submission.

        The path parameter is normalised to lower case and validated against
        the `Hex` pattern: a malformed hash is rejected with `400`, and only a
        well-formed hash with no matching record returns `404`.
      parameters:
        - name: hash
          in: path
          required: true
          description: |
            The canonical EIP-712 `intent_hash` that identifies this intent,
            as returned by `POST /intents` or computed locally by the SDK
            `hashIntent` function. Lookup is case-insensitive — the server
            normalizes the path parameter to lowercase before matching.
          schema:
            $ref: '#/components/schemas/Hash'
          example: "0x1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b"
      responses:
        '200':
          description: The intent record.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntentRecord'
        '400':
          description: |
            `hash` is not a well-formed `0x`-prefixed 32-byte hex string.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: "hash must be a 0x-prefixed 32-byte hex string"
        '404':
          description: |
            `hash` is well-formed but no intent with that hash exists in the
            mempool.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: "Intent not found"

  /intents/{hash}/status:

    patch:
      operationId: updateIntentStatus
      tags: [Intents]
      summary: Report a status transition for an intent
      description: |
        Restricted to trusted relayer/solver services that hold the
        mempool's shared bearer token (see "Status ownership" above).
        Refuses to change the status of an intent already in a terminal
        state (`settled`, `refunded`, `expired`).
      security:
        - statusToken: []
      parameters:
        - name: hash
          in: path
          required: true
          description: |
            The canonical EIP-712 `intent_hash`. Normalised to lower case
            before lookup; a value that is not a `0x`-prefixed 32-byte hex
            string is rejected with `400` (same validation as
            `GET /intents/{hash}`).
          schema:
            $ref: '#/components/schemas/Hash'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  $ref: '#/components/schemas/IntentStatus'
      responses:
        '200':
          description: Updated intent record.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntentRecord'
        '400':
          description: |
            `hash` is not a well-formed `0x`-prefixed 32-byte hex string, or
            `status` is missing / not one of the `IntentStatus` values.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: "hash must be a 0x-prefixed 32-byte hex string"
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: No intent with the given hash exists in the mempool.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: The intent is already in a terminal status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /info:

    get:
      operationId: getInfo
      tags: [Discovery]
      summary: Discover the EIP-712 domain this mempool instance verifies against
      description: |
        Returns the EIP-712 domain (`name`, `version`, `chainId`,
        `verifyingContract`) this mempool instance was configured with. A
        client can use this to confirm it is signing intents against the
        same chain/escrow the mempool will accept, without hard-coding the
        values out of band.
      responses:
        '200':
          description: The mempool's configured EIP-712 domain.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MempoolInfo'
              example:
                name: "Perihelion"
                version: "1"
                chainId: 8453
                verifyingContract: "0x0000000000000000000000000000000000000000"

components:
  securitySchemes:
    statusToken:
      type: http
      scheme: bearer
      description: Shared token configured on the mempool via `statusToken`.

  schemas:

    # ─── Primitive types ────────────────────────────────────────────────────

    Address:
      type: string
      description: |
        An EVM address: 20 bytes encoded as a `0x`-prefixed hex string,
        optionally EIP-55 checksummed.
      pattern: '^0x[0-9a-fA-F]{40}$'
      examples:
        - "0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045"
        - "0x0000000000000000000000000000000000000000"

    Hex:
      type: string
      description: |
        An arbitrary-length `0x`-prefixed hex string.
      pattern: '^0x[0-9a-fA-F]+$'
      examples:
        - "0x1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b"

    Hash:
      type: string
      description: |
        A 32-byte keccak256 hash, `0x`-prefixed: exactly 66 characters (`0x` +
        64 hex digits). Used for the canonical `intent_hash` — the mempool and
        on-chain identifier computed by the SDK `hashIntent` function. Unlike
        the general-purpose `Hex` type, this pattern is length-exact so a
        truncated or malformed hash is rejected at the schema level rather
        than accepted as "some hex string" (issue #530).
      pattern: '^0x[0-9a-fA-F]{64}$'
      examples:
        - "0x1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b"

    Signature:
      type: string
      description: |
        A canonical 65-byte `(r, s, v)` EIP-712 signature, `0x`-prefixed:
        exactly 132 characters (`0x` + 130 hex digits). 64-byte EIP-2098
        "compact" signatures are not accepted here — see `PerihelionEscrow._verify`
        and the SDK's `verifyIntent`, which apply the same length-exact rule
        (issue #530).
      pattern: '^0x[0-9a-fA-F]{130}$'
      examples:
        - "0x1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b0f1e2d3c4b5a69780f1e2d3c4b5a69780f1e2d3c4b5a69780f1e2d3c4b5a69781c"

    IntentStatus:
      type: string
      description: |
        Lifecycle state of an intent as tracked by this mempool. This is a
        subset of the full on-chain/SDK lifecycle — see the "Intent
        lifecycle" note above for `claimed`/`settling`, which this
        implementation never assigns.
      enum:
        - pending
        - settled
        - refunded
        - expired

    # ─── Core domain schemas ────────────────────────────────────────────────

    Intent:
      type: object
      description: |
        The canonical, signable description of what a user wants: spend a known
        asset/amount on a source EVM chain and receive at least `minDestAmount`
        of a target asset on Stellar before `deadline`.

        All numeric amounts are **decimal strings** in the asset's smallest
        unit to preserve full precision.
      required:
        - user
        - destination
        - sourceChainId
        - sourceAsset
        - sourceAmount
        - destAsset
        - minDestAmount
        - deadline
        - nonce
        - preferredSolver
      properties:
        user:
          $ref: '#/components/schemas/Address'
          description: EVM address of the user funding the source leg (the EIP-712 signer).
        destination:
          type: string
          description: |
            Stellar address that will receive the settled assets. Either a
            `G...` (account strkey) or `C...` (contract strkey), or `"native"`
            for the native XLM asset destination.
          examples:
            - "GBBD47IF6LWK7P7MDEVSCWR7DPUWV3NY3DTQEVFL4NAT4AQH3ZLLFLA5"
        sourceChainId:
          type: integer
          description: |
            EVM chain ID of the source network the user spends from.
            Common values: `1` (Ethereum mainnet), `8453` (Base),
            `42161` (Arbitrum One).
          examples:
            - 8453
        sourceAsset:
          $ref: '#/components/schemas/Address'
          description: ERC-20 token address being spent on the source chain.
        sourceAmount:
          type: string
          description: |
            Amount of `sourceAsset` the user locks, in the asset's smallest
            unit (decimal string, e.g. `"1000000"` for 1 USDC with 6 decimals).
          pattern: '^[0-9]+$'
          examples:
            - "1000000"
        destAsset:
          type: string
          description: |
            Asset the user wants to receive on Stellar.
            - `"native"` for XLM
            - `"<CODE>:<ISSUER_G...>"` for an issued asset
          examples:
            - "USDC:GA5ZSEJYB37JRC5AVCIA5MOP4RHTM335X2KGX3IHOJAPP5RE34K4KZVN"
            - "native"
        minDestAmount:
          type: string
          description: |
            Minimum acceptable amount of `destAsset` in its smallest unit
            (slippage floor). The solver must deliver at least this amount or
            the settlement is rejected.
          pattern: '^[0-9]+$'
          examples:
            - "9900000"
        deadline:
          type: integer
          description: |
            Unix timestamp (seconds) after which the intent is void and
            source-chain funds become refundable.
          examples:
            - 1751280000
        nonce:
          type: string
          description: |
            Unique value preventing replay or collision of otherwise-identical
            intents; encoded on-chain as a `uint256`. **Not** a `0x`-prefixed
            hex string in practice: the SDK's default generator
            (`randomNonce`) produces a base-10 decimal-digit string. A
            `0x`-prefixed hex string is also accepted on submission (the SDK
            parses it with `BigInt(...)`), but is not what callers get by
            default — treat this as "any string `BigInt()` can parse",
            not as `Hex`.
          pattern: '^(0x[0-9a-fA-F]+|[0-9]+)$'
          examples:
            - "95395590780195993686340100862393633005949636859136019347794446361576576451305"
        preferredSolver:
          $ref: '#/components/schemas/Address'
          description: |
            Optional solver address granted exclusive fill rights. Set to the
            zero address (`0x0000...0000`) for an open, competitive auction.

    SignedIntent:
      type: object
      description: |
        An `Intent` together with its EIP-712 signature and the canonical
        `intent_hash` that serves as the mempool and on-chain identifier.
      required:
        - intent
        - signature
        - hash
      properties:
        intent:
          $ref: '#/components/schemas/Intent'
        signature:
          $ref: '#/components/schemas/Signature'
          description: |
            EIP-712 signature over the intent by `intent.user`. Must be a
            valid 65-byte (`r`, `s`, `v`) compact hex string.
        hash:
          $ref: '#/components/schemas/Hash'
          description: |
            keccak256 EIP-712 commitment of the intent — the primary mempool
            and on-chain identifier. Computed by the SDK `hashIntent` function.
            The server recomputes this value itself from `intent`; a
            mismatched client-supplied `hash` is not currently rejected.

    IntentRecord:
      allOf:
        - $ref: '#/components/schemas/SignedIntent'
        - type: object
          description: |
            A `SignedIntent` enriched with lifecycle metadata maintained by
            the mempool server.
          required:
            - status
            - createdAt
          properties:
            status:
              $ref: '#/components/schemas/IntentStatus'
            solver:
              $ref: '#/components/schemas/Address'
              description: |
                EVM address of the solver that claimed the intent.
                Reserved for future use — no endpoint currently sets this
                field (see "Status ownership" above).
            settlementTx:
              type: string
              description: |
                Stellar transaction hash of the settlement operation.
                Reserved for future use — no endpoint currently sets this
                field (see "Status ownership" above).
              examples:
                - "a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2"
            createdAt:
              type: integer
              description: |
                Unix timestamp **in seconds** (`Math.floor(Date.now() / 1000)`) at which the
                record was created in the mempool.
              examples:
                - 1751270400

    # ─── Request / response envelopes ──────────────────────────────────────

    SubmitIntentResponse:
      type: object
      description: Successful response body from `POST /intents`.
      required:
        - hash
      properties:
        hash:
          $ref: '#/components/schemas/Hash'
          description: |
            The canonical `intent_hash` computed by the server.  Clients
            should verify this matches their locally-computed hash before
            storing the value.

    MempoolInfo:
      type: object
      description: |
        The EIP-712 domain this mempool instance verifies intent signatures
        against, as returned by `GET /info`.
      required:
        - name
        - version
        - chainId
        - verifyingContract
      properties:
        name:
          type: string
          description: EIP-712 domain name. Fixed to `"Perihelion"` by the SDK's `perihelionDomain` helper.
          examples:
            - "Perihelion"
        version:
          type: string
          description: EIP-712 domain version. Fixed to `"1"` by the SDK's `perihelionDomain` helper.
          examples:
            - "1"
        chainId:
          type: integer
          description: EVM chain ID this mempool instance's escrow is deployed on.
          examples:
            - 8453
        verifyingContract:
          $ref: '#/components/schemas/Address'
          description: PerihelionEscrow contract address this mempool instance verifies signatures against.

    ErrorResponse:
      type: object
      description: |
        Standard error envelope returned for 4xx and 5xx responses.
      required:
        - error
      properties:
        error:
          type: string
          description: Human-readable description of the error.
          examples:
            - "Missing intent or signature"
            - "Invalid signature"
            - "Intent not found"
            - "Invalid status filter: 'bogus' (expected one of pending, settled, refunded, expired)"
            - "Internal server error"
