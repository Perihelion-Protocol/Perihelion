{
  "name": "@perihelion/sdk",
  "version": "0.0.1",
  "description": "TypeScript SDK for the Perihelion intent-based cross-chain bridge",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import tsx test/*.test.ts",
    "test:coverage": "c8 --reporter=lcov --reporter=text --report-dir=coverage node --test --import tsx test/*.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "c8": "^10.1.2",
    "tsx": "^4.16.0",
    "typescript": "^5.4.0"
  }
}
